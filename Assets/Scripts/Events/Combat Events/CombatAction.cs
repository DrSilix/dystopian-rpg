using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// List of actions available during the combat event (not all actions necessarily combat related)
/// </summary>
public enum CombatAction
{
    Attack,
    Reload,
}
